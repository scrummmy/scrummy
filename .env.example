# Google Analytics 4 measurement ID (optional). Leave unset to run without GA —
# Cloudflare Web Analytics (see src/views/Privacy.vue) still works either way.
# Get one from https://analytics.google.com under Admin > Data Streams.
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
